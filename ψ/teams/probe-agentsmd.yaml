# ⛔ 2026-08-10 — prompt: บล็อกด้านล่าง **ไม่ถูกส่งเข้า pane บน maw-rs**
#    [verified: git show a162427: · team_up_apply.rs ไม่มีคำว่า prompt เลย ·
#     team_spawn.rs เขียนลงไฟล์ <vault>/<role>-spawn-prompt.md แล้วไม่อ้างถึงมันตอน spawn]
#    ⇒ กฎในบล็อกนี้ถึง seat **ก็ต่อเมื่อ** มีไฟล์กฎ (CLAUDE.md/AGENTS.md) บนดิสก์ที่ seat เดินถึงเอง
#    ⇒ ไม่ได้แก้ตรงนี้โดยตั้งใจ — การหย่อน AGENTS.md ลง worktree ทำให้ทีม *ผม* ทำงาน
#      แล้วปล่อยสมมติฐานของทั้งฟลีตไว้เหมือนเดิม (portia ปฏิเสธท่านี้ และเธอถูก)
#    ⇒ ตรวจ: verify-check.sh enginecheck <charter> → enginecheck.prompt-delivery:
# Probe charter — 2026-08-08
# Question: does a codex worker (a) read AGENTS.md at its cwd, and (b) see any skills?
# Engine is deliberately codex-sol = gpt-5.6-sol, the model this repo actually boots and the
# one whose include_skills_usage_instructions is False in models_cache.json. Testing any other
# model would answer a question nobody asked.
name: probe-agentsmd
project: arnon2020/codex-fanout
session: 114-codex-fanout

goal: |
  One codex worker, one probe, no code changes. Report to lead and stop.

members:
  # probe-1 retired 2026-08-08 after PROBE-8F2A answered. Left commented, not deleted:
  # preflight is a CHARTER-level check, so leaving it declared made probe-2 fail on
  # "CODEX_HOME isolation: probe-1+probe-2 share /home/user/.codex" — the exact collision
  # this repo documents. Two codex members need two CODEX_HOMEs, or one at a time.

  # probe-2 exists because PROBE-8F2A could not answer the question it was built for.
  # Asking probe-1 "is there a file named AGENTS.md" made it go and read the file, which proves
  # reachable, not auto-injected. probe-2 gets a question that names no file, and must be FRESH —
  # probe-1's context now holds the whole document.
  # probe-3: re-probe at 160 lines. PROBE-C4D1 proved injection of the 90-line file; that proof
  # does not carry across a 78% growth. Questions come from sections added AFTER C4D1, so a
  # correct answer proves injection at the new size AND that the new prose is legible.
  - role: probe-3
    name: probe-3
    engine: codex-sol
    worktree: agents/probe-3
    branch: agents/probe-3
    prompt: |
      You are a probe. Do NOT write code, do NOT commit, do NOT change any file.
      WAIT for your question via maw hey, answer it exactly, then stop.
      Report to: 114-codex-fanout:codex-fanout
      END EVERY TURN WITH SOMETHING IN FLIGHT. Before you stop, at least one must be true:
      1. you dispatched an agent AND it is consuming — check the pane; delivered != consumed
      2. a script or background job is running
      3. a watcher is armed, so the next event wakes you
      4. the turn genuinely ends the program — and you SAY SO explicitly
      Prose is a report ON work, never a substitute FOR it. If your analysis produced no next
      action, that IS the finding: say it plainly rather than letting silence imply progress.
      Do not generate work by replying — when a counterpart says nothing is outstanding, the
      next action is the task, not a better acknowledgement.
      [scaffold for a measured failure — retire when turns reliably end in flight without it]
  # probe-2 retired after PROBE-C4D1 — two codex members share one CODEX_HOME (see section 3).
  # - role: probe-2  (retired)
  #   name: probe-2
  #   engine: codex-sol
  #   worktree: agents/probe-2
  #   branch: agents/probe-2
  #   prompt: |
  #     You are a probe. Do NOT write code, do NOT commit, do NOT change any file.
  #     WAIT for your question via maw hey, answer it exactly, then stop.
  #     Report to: 114-codex-fanout:codex-fanout

  # No lead member on purpose: the lead (114-codex-fanout:codex-fanout) is already live.
  # Declaring it here made preflight fail on ".maw-engine: lead has no worktree to inspect"
  # and would have risked spawning a second claude over the running session.

lifecycle:
  worktree: true
  merge_on_shutdown: false
